# StartAI — 会话日志记录指令

> 依赖环境变量：执行 `source $Start_Set/Set_Path.sh` 导入所需变量。
> - `$Start_Set`：Start_Set 配置目录（`~/Start_Set`）
> - `$AI_log`：日志根目录（`~/AI/log`）
> - `$AI_script`：脚本存放根目录（`~/AI/scripts`）
> - `$SOFT_HELP`：软件 Help 文档根目录（`~/SoftWare/Help`）
> - `$git_demand_record`：Git 操作记录根目录（`~/git_demand_record`）
> - `$Temp_Test`：临时测试目录（`~/Test_Dir`）
> - `$Fun_Open`：功能开关配置文件（`~/Start_Set/skills/function_switch.md`）
> - `$Fun_Open`：功能开关配置文件（`~/Start_Set/skills/function_switch.md`）

### 环境变量管理

- **新增环境变量必须写入 `$Start_Set/Set_Path.sh`**，格式为 `export <变量名>=<路径>`
- **同时必须在本文档（`StartAI.md`）的环境变量列表中追加对应条目**，注明变量名和用途
- 两份文件（`Set_Path.sh` 和 `StartAI.md`）保持同步，任意新增变量后同时更新两者

### 环境变量管理

- **新增环境变量必须写入 `$Start_Set/Set_Path.sh`**，格式为 `export <变量名>=<路径>`
- **同时必须在本文档（`StartAI.md`）的环境变量列表中追加对应条目**，注明变量名和用途
- 两份文件（`Set_Path.sh` 和 `StartAI.md`）保持同步，任意新增变量后同时更新两者

## 核心指令

**每次会话中的所有问题和回答，必须追加记录到 `$AI_log/` 下当天的日志文件中，包含问题、答案和思考过程。每天顺序写入同一个文件，不在每次会话时创建新文件。**

## 技能加载机制

`$Start_Set/skills/` 下存放按领域分类的技能文件。AI 不得在每次会话中无条件加载全部 Skill，而是根据当前任务的触发条件按需加载。

- Skill 文件顶部包含 `**触发条件**` 字段，声明该技能的适用场景
- 用户请求到达后，先判定该请求是否命中某个 Skill 的触发条件
- 命中则读取对应 Skill 文件并按其中规则执行，未命中则忽略
- 一个任务可能同时命中多个 Skill，此时全部加载
- 当前已有的 Skill 及触发条件：
  - `script-development.md`：编写/调试/修改脚本
  - `git-workflow.md`：执行 Git 操作（commit/push/pull/merge 等）
  - `doc-processing.md`：处理 MD 表格/图片下载/CSV 转换/Verilog 格式化等
- 新增 Skill 时必须在文件中声明触发条件，并在此处同步更新列表

---

# 一、通用规则（所有会话均适用）

## 1.1 日志类

### 1.1.1 日志目录结构

```
$AI_log/
└── YYYY/
    └── MM/
        └── DD/
            ├── log_001.md
            ├── log_002.md
            └── ...
```

- 年份目录：`YYYY`（如 `2026`）
- 月份目录：`MM`（如 `07`）
- 日期目录：`DD`（如 `23`）
- 日志文件：`log_{三位序号}.md`，从 `log_001.md` 开始
- **同一文件追加写入，不按会话分文件**

### 1.1.2 文件拆分规则

- 每个 `log_NNN.md` 最多 **10 万行**
- 追加前先检查当前最新文件行数
- 若当前文件行数 + 待追加内容 > 10 万行，则创建下一个序号的文件（如 `log_002.md`），从新文件开始写入
- 10 万行以内始终追加到当前最新文件末尾

### 1.1.3 追加格式

每次会话追加时，在已有内容后添加分隔标记和本次会话记录：

```markdown
---

## 会话 N — YYYY-MM-DD HH:MM:SS

主题：{本会话主题简述}

### 第 1 轮

**用户 [HH:MM]**

> {用户原始问题/指令}

**思考**

{AI 的分析思路、探索步骤、关键判断}

**回答**

{AI 的实际回答、执行的操作、修改的文件等}

### 第 2 轮

...
```

- 每次会话以 `---` 分隔线开头
- 时间精确到分钟即可
- 思考过程要完整记录（读取了哪些文件、做了哪些推理、关键决策点）
- 如果涉及代码修改，记录修改了什么文件和为什么修改
- 如果生成或创建了文件，列出文件路径和用途

### 1.1.4 执行时机

- **每次回复用户后**：将本轮对话追加到当天的日志文件
- 追加前检查目录结构是否存在，按需创建 `YYYY/MM/DD/`
- 追加前检查当前文件行数，必要时创建新文件

### 1.1.5 日志注意事项

- 追加写入，不覆盖已有内容
- 短交互（如仅打招呼）也应记录
- 技术主题的会话需要保留完整的文件路径、命令、代码片段信息
- 每天从 `log_001.md` 开始，累计使用
- 路径引用优先使用环境变量（`$AI_log`、`$AI_script`）而非硬编码绝对路径

## 1.2 多步骤处理类

### 1.2.1 分步验证与缓存中间结果

- 任何多步骤操作必须逐步验证，不可一气呵成写完所有代码再统一调试
- 第一步验证通过后，将其产出（数据文件、中间脚本等）持久化保存
- 调试后续步骤时直接复用已缓存的中间结果，不得重复执行已通过验证的前置步骤
- 只有当某步骤的输入条件发生变化时才需重新执行该步骤
- 例如：生成随机测试数据 → 缓存为文件；调试验证脚本 → 用同一份缓存数据反复调试，而非每次重新生成

### 1.2.2 安装软件前须征得用户同意

- 任何任务中需要安装新软件（包括 apt、snap、pip、npm、二进制下载等方式）时，不可直接执行安装命令
- 必须先向用户说明：需要安装什么软件、用途是什么、使用哪种安装方式
- 只有在用户明确同意后，才可以执行安装操作
- 此规则优先级高于场景规则 2.2（安装软件类）中的所有条款——未获用户同意，2.2 中的任何安装步骤均不得启动

### 1.2.3 修改文件后自检提交与推送

- 每次修改 `$Start_Set/` 或 `$AI_script/` 下的文件后，回复用户之前必须逐项自问并完成：
  1. 是否已 `git add` + `git commit`？
  2. 是否已 `git push`？
  3. 是否已记录到 `$git_demand_record/`？
- 以上三项全部完成，才可以回复用户。缺任何一项均为违规，不得以"等用户提醒再补"为理由延后
- 此规则高于 2.3 中的即时推送条款——2.3 定义了"什么目录要推送"，本规则定义了"推送是回复的前置条件"

### 1.2.4 测试中间文件管理

- 所有测试过程中产生的临时文件、目录、数据统一放在 `$Temp_Test/` 下
- **防串扰**：在 `$Temp_Test/` 下创建专属子目录（如 `$Temp_Test/<任务名>_<时间戳>/`），避免多个终端/会话同时访问同一路径导致冲突
- **用完即清**：测试完成后：
  1. 需要归档的产物（如验证脚本、对比数据）存入对应项目的持久目录
  2. 其余临时文件连同专属子目录一并删除
- 禁止在 `$Temp_Test/` 下遗留临时文件，回复用户前检查是否已清理完毕

#### 1.2.4.1 批量任务脚本的归档

- 当通过脚本完成批量任务（如批量生成、批量转换、批量格式处理等）时，**脚本本身具有复用价值，不可随临时文件一起丢弃**
- 将脚本归档到 `$AI_script/<分类>/` 下，命名体现其功能（如 `gen_sram_frontend.py`）
- 归档后同步更新对应分类目录的 README.md，写入脚本说明
- 判断标准：如果同一类任务可能在后续再次执行（只是参数不同），则该脚本应归档；仅当脚本完全针对一次性场景、参数硬编码且无通用性时才可丢弃

### 1.2.5 重复性任务与中间脚本

**触发条件**：当任务涉及批量生成、批量补全、批量转换等重复性操作时。

#### 1.2.5.1 参数化优先

- 重复性任务必须使用**参数化脚本**完成，不得手动逐个操作或使用硬编码一次性脚本
- 脚本应接受命令行参数或函数参数来控制输入/输出/配置

#### 1.2.5.2 中间脚本的功能测试

- 中间任务产生的脚本必须进行功能测试，开关由 `$Fun_Open` 中的 `TEST_INTERMEDIATE_SCRIPT` 控制（默认 `on`）
- 测试可信度要求由 `TEST_CONFIDENCE` 定义（默认 `90`，单位 %）
  - 可信度 = 测试用例通过数 / 总测试用例数 × 100%
  - 测试用例需覆盖：正常输入、边界值、异常输入
- 未达到可信度要求时不得交付，必须先修复后重新测试

#### 1.2.5.3 开关控制

- 是否测试、测试可信度阈值、是否归档 均由 `$Fun_Open` 中的开关控制
- 执行前先读取 `$Fun_Open` 获取当前开关值
- 新增开关必须在 `$Fun_Open` 中定义并写入默认值

### 1.2.5 重复性任务与中间脚本

**触发条件**：当任务涉及批量生成、批量补全、批量转换等重复性操作时。

#### 1.2.5.1 参数化优先

- 重复性任务必须使用**参数化脚本**完成，不得手动逐个操作或使用硬编码一次性脚本
- 脚本应接受命令行参数或函数参数来控制输入/输出/配置

#### 1.2.5.2 中间脚本的功能测试

- 中间任务产生的脚本必须进行功能测试，开关由 `$Fun_Open` 中的 `TEST_INTERMEDIATE_SCRIPT` 控制（默认 `on`）
- 测试可信度要求由 `TEST_CONFIDENCE` 定义（默认 `90`，单位 %）
  - 可信度 = 测试用例通过数 / 总测试用例数 × 100%
  - 测试用例需覆盖：正常输入、边界值、异常输入
- 未达到可信度要求时不得交付，必须先修复后重新测试

#### 1.2.5.3 开关控制

- 是否测试、测试可信度阈值、是否归档 均由 `$Fun_Open` 中的开关控制
- 执行前先读取 `$Fun_Open` 获取当前开关值
- 新增开关必须在 `$Fun_Open` 中定义并写入默认值

## 1.3 输出验证类

### 1.3.1 先验证再输出

- 任何分析结果、搜索结果、代码产出、配置修改等，在呈现给用户之前必须先自行验证其正确性，不可直接将未验证的结论输出
- 验证方式包括但不限于：执行测试、检查语法、核对数据、复现步骤
- 若验证涉及权限问题导致无法直接执行，须先向用户确认，不得绕过权限限制

### 1.3.2 验证产物归档

- 验证过程中生成的辅助脚本（如测试脚本、一次性检查脚本），按场景规则 2.1 脚本类规范存放到 `$AI_script/` 对应分类目录
- 验证过程中产生的中间数据、测试结果等，存放到对应项目的数据目录（如与原始数据同目录的 `result/` 下）
- 不可验证完即丢弃，以便后续复现和追溯

---

# 二、场景规则（仅特定任务适用）

**触发条件**：当任务涉及以下场景时，自动应用对应规则。

## 2.1 脚本类

**触发场景**：需要编写、生成、或维护代码/脚本（Python、Shell、Makefile 等）时。

### 2.1.1 脚本存放

- 所有生成的脚本统一放置在 `$AI_script/` 下
- `$AI_script/` 内按功能/语言分子目录，例如：
  ```
  $AI_script/
  ├── python/       # Python 脚本
  ├── bash/         # Shell 脚本
  ├── makefile/     # Makefile 相关
  └── ...           # 其他按需创建
  ```
- 新建脚本时，先在 `$AI_script/` 下确认对应分类目录存在，不存在则创建

### 2.1.2 脚本编码

- **必须包含 `Main` 函数**：作为脚本入口，负责参数解析和主流程调度
- **其他函数按功能切分**：每个函数职责单一，命名清晰反映其功能
- 典型结构：
  ```python
  def helper_a():
      ...

  def helper_b():
      ...

  def Main():
      # 解析参数 → 调用功能函数 → 输出结果
      ...

  if __name__ == "__main__":
      Main()
  ```

### 2.1.3 README 维护

- **每次在 `$AI_script/` 下生成新脚本时，必须在对应分类目录（如 `python/`、`bash/`）下创建或更新 `README.md`**
- **README 更新是脚本创建的闭环步骤，不可省略或延后**：完成脚本代码编写和测试后，**在同一轮回复中立即更新 README**。不可先交付脚本、等用户提醒再补 README。脚本创建任务以"代码完成 + README 更新"为结束标志
- README.md 内容要求：脚本名称、作用、使用方式、依赖、函数说明
- 若该分类目录下已存在 `README.md`，则在其末尾追加新脚本的说明（不覆盖已有内容）
- 追加格式：
  ```markdown
  ---

  ## {脚本名称}

  **作用**：...

  **使用方式**：
  ```
  ...
  ```

  **函数说明**：
  - `Main`：参数解析与主流程调度
  - `helper_a`：...
  - `helper_b`：...

  **依赖**：...
  ```

### 2.1.4 README 禁止覆盖

- **更新已有 README 时，必须先用 `read` 读取现有内容，再用 `edit` 追加新条目**
- 禁止用 `write` 覆盖已有 README（`write` 仅用于该分类目录下首次创建 README）
- 违反此规则将导致该目录下其他脚本的文档丢失

### 2.1.5 新建脚本后自检 README

- 每次用 `write` 在 `$AI_script/` 下创建新的 `.py`/`.sh`/`.mk` 等脚本文件后，回复用户之前必须自问："该分类目录的 README 是否已更新？"
- 如果答案为否，必须立即追加后才可以回复用户

### 2.1.6 运行环境检查

- 编写脚本前先确认目标环境的运行时版本和可用工具
- 避免使用当前环境不支持的语法或 API（如 Python 3.8 不支持 `list[str]`，需用 `typing.List`）

### 2.1.7 输入即契约

- 不硬编码任何可从输入数据推导的信息：列名从数据解析、列数自动统计、缺值占位数按实查列宽决定
- 脚本服务于"此类数据"而非"当前这份数据"，不得预设输入规模和具体取值

#### 2.1.7.1 合并前验证行数

- 两表合并前先比对数据行数
- 不一致时定位差异行并显式告知用户，不静默截断或补齐

### 2.1.8 输出格式遵守结构约定

- 输出不仅内容须正确，格式的结构约束也必须满足
- 不同格式有各自的合法结构要求，不可只关注内容而忽视格式

#### 2.1.8.1 按格式处理细节

- Markdown 表格：分隔行 `---` 宽度 = 最大列宽 + 2（两侧空格），否则渲染列不对齐
- CSV：字段内包含分隔符或换行时须正确转义
- JSON：键名必须唯一，特殊字符须转义
- YAML：正确处理缩进层级，避免 Tab 与空格混用
- 具体约束依实际输出格式而定，以上为常见示例

### 2.1.9 内容感知对齐

- 表格列的对齐方式由该列的内容类型决定，类型通过采样输入数据自动检测，不预设某列为何种类型
- 通用原则：表头行统一左对齐；数据行按内容类型——文本左对齐，数值右对齐

#### 2.1.9.1 自动检测策略

- 对每列采样若干数据行
- 若 >50% 的非占位值可解析为目标类型，则判定为该类型列
- 例如：尝试 `float(s)` 成功即视为数值列

### 2.1.10 性能优化不覆盖原脚本

- 对已有脚本进行性能优化时，**不直接覆盖原脚本**
- 新建文件，命名为 `<原脚本名>_perf.<扩展名>`（如 `find_sram_sv.sh` → `find_sram_sv_perf.sh`）
- 在对应分类目录的 README 中，`_perf` 条目必须写明：
  - 与原脚本实现方式的不同点
  - 为什么性能提升（算法/系统调用/数据结构层面的原因）
  - 性能对比数据表格（测试规模、原版耗时、优化版耗时、提升倍数）

### 2.1.10 性能优化不覆盖原脚本

- 对已有脚本进行性能优化时，**不直接覆盖原脚本**
- 新建文件，命名为 `<原脚本名>_perf.<扩展名>`（如 `find_sram_sv.sh` → `find_sram_sv_perf.sh`）
- 在对应分类目录的 README 中，`_perf` 条目必须写明：
  - 与原脚本实现方式的不同点
  - 为什么性能提升（算法/系统调用/数据结构层面的原因）
  - 性能对比数据表格（测试规模、原版耗时、优化版耗时、提升倍数）

## 2.2 安装软件类

**触发场景**：需要通过包管理器（apt、snap、pip 等）或下载工具（curl、wget）安装/下载软件时。

- 保留下载和安装的进度显示，禁止使用静默选项：
  - `apt install` 不加 `-qq`、`-q`
  - `curl` 保留默认进度或使用 `--progress-bar`
  - `wget` 保留默认进度条
  - `pip install` 保留默认输出
  - `snap install` 保留默认输出
- 不得将输出重定向到 `/dev/null` 或以其他方式隐藏进度

### 2.2.1 软件 Help 文档归档

- 安装软件后，将软件的 Help 文档保存到 `$SOFT_HELP/` 目录下
- 在 `$SOFT_HELP/` 下按软件名创建子目录（如 `$SOFT_HELP/glow/`）
- 在该子目录下存放：
  - 英文版 Help（原始输出，如 `help_en.txt` 或 `help_en.md`）
  - 中文版 Help（翻译版本，如 `help_cn.md`）
- 目录结构示例：
  ```
  $SOFT_HELP/
  └── glow/
      ├── help_en.txt
      └── help_cn.md

### 2.2.2 安装前检查

- 安装前先检查软件是否已安装（`which <tool>` 或 `<tool> --version`），避免重复安装
- 检查必要的前置条件：权限（是否有 sudo）、磁盘空间、网络连通性

### 2.2.3 权限与备选方案

- 当 `sudo` 不可用时，不得静默失败后跳过
- 应向用户报告权限不足的具体错误，并询问：提供密码继续、或尝试备选方案
- 备选方案优先级：snap > 用户级 pip（`--user`）> 二进制下载 > 手动编译

### 2.2.4 安装后验证

- 安装完成后必须运行版本命令或最小功能测试确认成功
- 示例：`<tool> --version`、`<tool> --help`、或执行一次基础操作
- 不可假定安装命令返回成功即代表安装正常

### 2.2.5 错误处理

- 安装失败时不得自行跳过、更换方案、或静默忽略
- 必须先向用户报告具体错误信息，等待确认后再决定下一步

### 2.2.6 安装过程记录

- 安装过程须记录完整日志，保存到 `$SOFT_HELP/<软件名>/install_log.md`
- 记录内容：
  - 安装时间、环境信息（OS 版本、内核版本）
  - 执行的每一条命令及其输出摘要
  - **着重记录错误原因和最终解决方案**（如依赖缺失、版本不兼容、权限问题等）
- 目的：确保下一次安装该软件时可直接复用成功的方案，避免重复踩坑
- 示例：
  ```
  $SOFT_HELP/
  └── gh/
      ├── help_en.txt
      ├── help_cn.md
      └── install_log.md
  ```
  ```

## 2.3 Git 操作类

**触发场景**：需要将代码/文件推送到 GitHub 远程仓库时。

- 统一使用 SSH 方式连接 GitHub
- 仓库名默认使用文件夹名称
- SSH 地址格式：`git@github.com:fmrtdragon/<仓库名>.git`
- **Git 操作记录**：每次执行 git 操作命令（commit、push、pull、merge、rebase 等）后，将执行的命令追加记录到 `$git_demand_record/` 下当天的日志文件中
  - 目录结构：`$git_demand_record/YYYY/MM/DD/git_log.md`，同一天追加到同一文件
  - 记录格式：时间戳 + 仓库路径 + 完整命令 + 操作结果摘要
  - 目的：确保所有 git 操作可追溯
- **推送前隐私检查**：每次 push 之前必须扫描待推送内容，检查是否包含个人信息（用户名、邮箱、绝对路径 `/home/<user>/` 等）
  - 若发现个人信息，必须先向用户确认是否允许推送，不得直接推送
  - GitHub 仓库名（如 `fmrtdragon`）不计入个人信息
- **Git 错误处理**：当 Git 操作遇到错误时（如远端仓库不存在、push 被拒绝、认证失败等），不得自行决定补救措施（如创建远端仓库、强制推送、修改 remote 等），必须先向用户报告错误并等待确认后再继续
- **即时推送**：当修改涉及以下目录下的文件时，commit 后应主动执行 push，无需等待用户单独下推送指令。但隐私检查规则（上述）仍然适用：若发现个人信息，必须先向用户确认
  - `$Start_Set/`
  - `$AI_script/`
